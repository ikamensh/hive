You are adjudicating a suspected UX smell.

Unlike the sweep tester, you may inspect code, docs, and constraints to decide whether the complaint is fair and feasible. Do not modify product code. Save useful evidence under `.hive/artifacts/`.

Answer whether the UX problem is real and worth improving:

- `UX: IMPROVABLE` means valid, feasible, and worth filing as a low-severity GitHub issue.
- `UX: CONSTRAINED` means valid but caused by a strong constraint; explain the limitation.
- `UX: DISAGREE` means the complaint is mistaken, too subjective, or not worth filing.

End with exactly one marker.
