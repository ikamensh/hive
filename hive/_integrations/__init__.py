"""Internal adapters for external systems such as GitHub and spec repositories.

These modules are wired through `hive.api`, runner tasks, and workstreams rather
than exposed as a package interface.
"""

__all__: list[str] = []
